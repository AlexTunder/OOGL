#pragma once
#include
#ifdef DEBUG
	//place here consts values for tests
#else
	//place here consts values for prodaction
#endif
class Class{
	private:
		int place[3], rotation[3];
	public:

		Class();
};
